networks:
  ai_network:
    driver: bridge

services:
  heygem-web:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: heygem-web
    restart: always
    ports:
      - '8090:8090'
    volumes:
      - ${DIGITAL_PEOPLE_DIR:-~/heygem_data/digitalpeople}:/root/windows-share
      - ./client/dist:/root/client/dist
      # 使用仓库内的 users.json 覆盖默认用户配置
      - ./server/data/users.json:/root/data/users.json:ro
    environment:
      - APP_PORT=8090
      - DIGITAL_PEOPLE_DIR=/root/windows-share
      - APP_WORKDIR=/root/windows-share/workdir
      - HOST_VOICE_DIR=/root/windows-share/voice/data
      - HOST_VIDEO_DIR=/root/windows-share/face2face
      - HOST_RESULT_DIR=/root/windows-share/face2face/result
      - WIN_COMPANY_DIR=/root/windows-share
      - TTS_BASE_URL=http://heygem-tts:8080
      - VIDEO_BASE_URL=http://heygem-gen-video:8383
      - GEN_VIDEO_CONTAINER=heygem-gen-video
      - GEN_VIDEO_CONTAINER_DATA_ROOT=/code/data
      - STATIC_DIR=/root/client/dist
    depends_on:
      - heygem-tts
      - heygem-gen-video
    networks:
      - ai_network
  heygem-tts:
    image: guiji2025/fish-speech-ziming
    container_name: heygem-tts
    restart: always
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=0
      - NVIDIA_DRIVER_CAPABILITIES=compute,graphics,utility,video,display
    ports:
      - '18180:8080'
    volumes:
      - ${DIGITAL_PEOPLE_DIR:-~/heygem_data/digitalpeople}/voice/data:/code/data
    command: /bin/bash -c "/opt/conda/envs/python310/bin/python3 tools/api_server.py --listen 0.0.0.0:8080"
    networks:
      - ai_network
  heygem-asr:
    image: guiji2025/fun-asr
    container_name: heygem-asr
    restart: always
    runtime: nvidia
    privileged: true
    working_dir: /workspace/FunASR/runtime
    ports:
      - '10095:10095'
    command: sh /run.sh
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    networks:
      - ai_network
  heygem-gen-video:
    image: guiji2025/heygem.ai
    container_name: heygem-gen-video
    restart: always
    runtime: nvidia
    privileged: true
    volumes:
      - ${DIGITAL_PEOPLE_DIR:-~/heygem_data/digitalpeople}/face2face:/code/data
    environment:
      - PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    shm_size: '8g'
    ports:
      - '8383:8383'
    command: python /code/app_local.py
    networks:
      - ai_network
